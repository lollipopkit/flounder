# Expected Fail: Unfaithful Verifier PoC

This artifact should not score as a prompt-regression success. The claimed
exploit only works after a trusted owner installs a no-code verifier or an
accept-anything verifier. That path does not exercise the actual vulnerable code
path with attacker-real capabilities, and it does not show a settlement count
outside the verified statement.
